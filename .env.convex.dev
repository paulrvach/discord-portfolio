# Used only by scripts/import-dev-to-prod.sh to export from dev.
# Use only the deployment name (no deploy key after "|"). Auth uses your login from `npx convex dev`.
CONVEX_DEPLOYMENT=dev:helpful-kingfisher-649
